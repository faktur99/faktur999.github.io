// Local stub for imagesLoaded
function imagesLoaded(elem, callback) {
  try {
    // immediately call back for static pages
    if (typeof callback === 'function') callback();
  } catch (e) {
    console.warn('imagesLoaded fallback error', e);
  }
}
window.imagesLoaded = window.imagesLoaded || imagesLoaded;
