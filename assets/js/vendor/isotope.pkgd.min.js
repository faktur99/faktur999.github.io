// Local stub for Isotope
function Isotope(container, options) {
  console.info('Isotope fallback instantiated for', container, options);
  this.container = container;
  this.arrange = function(opts) {
    console.info('Isotope.arrange called with', opts);
  };
}
window.Isotope = window.Isotope || Isotope;
