// Local stub for Swiper
function Swiper(container, config) {
  console.info('Swiper fallback instantiated for', container, config);
  this.container = container;
}
window.Swiper = window.Swiper || Swiper;
