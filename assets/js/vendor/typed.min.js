// Local fallback for Typed.js (minimal stub)
function Typed(selector, options) {
  console.info('Typed fallback instantiated for', selector);
  this.strings = options && options.strings ? options.strings : [];
}
window.Typed = window.Typed || Typed;
